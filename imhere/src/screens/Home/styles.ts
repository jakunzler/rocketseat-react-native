import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#992828',
    alignItems: 'center',
    justifyContent: 'center',
  },
  firstParagraph: {
    color: '#fff',
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 10
  },
  secondParagraph: {
    color: '#cdcdcd',
    fontSize: 20
  }
});
